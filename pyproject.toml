[build-system]
requires = ["setuptools>=42", "wheel", "pybind11>=2.10"]
build-backend = "setuptools.build_meta"

[project]
name = "og_env"
version = "1.0.0"
description = "Python bindings for Overgrowth RL Environment"
requires-python = ">=3.7"
dependencies = ["numpy"]

[tool.setuptools]
py-modules = []
packages = []
